name: development pipeline

on:
  push:
    branches: ["develop"]

jobs:
    
  build-image:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v6

      - name: build docker image
        run: docker build -t hackage-doc-mcp:0.0.1 .
